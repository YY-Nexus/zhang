# 📋 服务器上需要执行的命令

**当前状态：** 项目文件还未上传到服务器

---

## 🎯 完整流程

### 第一步：在本地电脑上传文件

在**本地电脑**（Mac）执行：

```bash
cd /Users/yanyu/Documents/zhang

# 方式1：使用rsync（推荐）
rsync -avz --progress \
    --exclude 'node_modules' \
    --exclude '.next' \
    --exclude '.git' \
    --exclude '.DS_Store' \
    --exclude '*.log' \
    --exclude '.trae' \
    ./ root@47.94.135.202:/opt/wedding-site/

# 输入密码：My151001
```

或者使用快速上传脚本：

```bash
cd /Users/yanyu/Documents/zhang
bash quick-upload.sh
```

---

### 第二步：在服务器上执行部署

连接服务器后，执行：

```bash
# 1. 进入项目目录
cd /opt/wedding-site

# 2. 执行部署脚本（会自动安装环境、构建项目、配置Nginx和SSL）
bash scripts/server-setup.sh
```

---

### 第三步：如果 SSL 证书有问题

```bash
cd /opt/wedding-site
bash scripts/fix-ssl.sh
```

---

## ⚠️ 如果目录不存在

如果服务器上 `/opt/wedding-site` 目录不存在，rsync 会自动创建。

如果手动创建：

```bash
# 在服务器上执行
mkdir -p /opt/wedding-site
chmod 755 /opt/wedding-site
```

---

## 📝 服务器上完整命令清单

```bash
# 1. 创建目录（如果不存在）
mkdir -p /opt/wedding-site

# 2. 进入目录
cd /opt/wedding-site

# 3. 查看文件是否上传成功
ls -la

# 4. 执行部署脚本
bash scripts/server-setup.sh

# 5. 如果SSL有问题，执行修复
bash scripts/fix-ssl.sh

# 6. 查看应用状态
pm2 status
pm2 logs wedding-site

# 7. 查看Nginx状态
systemctl status nginx

# 8. 测试HTTPS
curl -I https://zhang.0379.love
```

---

**现在先在本地电脑上传文件，然后再在服务器上执行部署！** 🚀
